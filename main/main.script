
function init(self) 
    if  gamecenter~= nil then
    	print("connect native code ========> authenticate..")
        gamecenter.authenticate();
    else
        print("could not connect native code")
    end
end

function final(self)
end


function update(self, dt)
end
